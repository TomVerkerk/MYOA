using UnityEngine;
using System.Collections;

public class ModifyText : MonoBehaviour {

//	public GUIText TextObject;
	public GUI gui;
	public 

	// Use this for initialization
	void Start () {
		//TextObject.guiText.text = ""+ Screen.width + "/" + Screen.height;
		
		//Text.guiText.text = ""+ Screen.width + "/" + Screen.height;
		//Text.
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void OnGUI(){
	//	GUI.TextArea (new Rect(new Vector3(Screen.width / 2, Screen.height / 2)), "" + Screen.width + "/" + Screen.height,0);
	}
}
